# OS
.DS_Store
Thumbs.db

# Python (for future IoT scripts)
__pycache__/
*.pyc
.venv/
venv/

# Node (if we go that route for dashboards)
node_modules/

# Secrets / credentials
.env
*.secret

# Sensor data (may be large, track in separate system)
data/*.csv
data/*.db
